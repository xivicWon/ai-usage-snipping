import XCTest
@testable import ClaudeMonitor

final class ClaudeMonitorTests: XCTestCase {
    func testPlaceholder() throws {
        // Placeholder test — replaced in later tasks
        XCTAssertTrue(true)
    }
}
